{
  "name": "fth-x402-mesh-pulse",
  "version": "0.1.0",
  "private": true,
  "description": "FTH x402 Mesh Pulse — neural network controller. Keeps electricity flowing through the mesh: cascade appreciation, market signals, agent orchestration, real-time WebSocket state.",
  "main": "dist/fth-x402-mesh-pulse/src/index.js",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "start": "node --enable-source-maps dist/fth-x402-mesh-pulse/src/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.8.4",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/pg": "^8.11.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.10.0",
    "typescript": "^5.4.0"
  }
}
