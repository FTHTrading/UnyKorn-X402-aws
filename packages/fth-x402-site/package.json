{
  "name": "fth-x402-site",
  "version": "0.1.0",
  "private": true,
  "description": "Information site for x402.unykorn.org",
  "scripts": {
    "deploy": "npm run deploy:preview",
    "deploy:preview": "npx wrangler pages deploy public --project-name x402-site --commit-dirty true",
    "deploy:production": "npx wrangler pages deploy public --project-name x402-site --branch main --commit-dirty true",
    "guardian:test": "node scripts/guardian-sign-test.mjs",
    "guardian:test:bip340": "node scripts/guardian-sign-test.mjs --bip340"
  },
  "dependencies": {
    "@noble/curves": "^1.9.1",
    "@noble/hashes": "^1.8.0",
    "@stellar/stellar-sdk": "^13.3.0",
    "@x402/core": "^0.0.1",
    "@x402/express": "^0.0.1",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "nodemailer": "^6.10.1",
    "viem": "^2.52.0",
    "xrpl": "^4.6.0"
  }
}
