# Security review findings recorded 2026-09-16 (automated review during the multi-rail change)

Not running, not served (verified: PM2 runs only site-server.js and task-server.js; listeners are 127.0.0.1:3101 and :4402; companion.html 404 on twin.unykorn.org and genesis402.com):
- mcp-server.js `/mcp/invoke`: unauthenticated treasury-signing tools (bridge/swap) and a fail-open x402 gate. DO NOT START this file until it binds 127.0.0.1, checks a bearer with timingSafeEqual, verifies x402 before every PAID_TOOL, and caps amounts per tool.
- public/companion.html + public/eve.html: innerHTML of LLM replies (XSS). Fix with textContent before these pages are ever served.
- routes/onboarding-api.js `/api/namespace/claim`: email from body, no session (IDOR). Fix by deriving identity from a verified session before mounting.

The multi-rail lanes (_ops_lanes.cjs, task-server.js) were not part of these findings.
